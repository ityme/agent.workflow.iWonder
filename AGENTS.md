# AGENTS.md

## 交互规则

- 永远使用中文与用户交互。
- 接到新任务时，先给出简短建议或计划，再进入具体修改。
- 需求确认阶段必须先整理范围、变更点、验收标准、非目标和未决问题，确认完成后才能进入任务拆解或实现。
- 任何代码更改前，必须先说明将要改动的内容并征得用户确认，除非用户明确要求直接修改。
- 完成代码更改后，必须输出改动摘要，包含修改文件、关键变更和已知风险。
- 默认使用当前可用的最佳模型；只有在明确切换时才使用轻量模型。
- 对于不合理、冲突、信息不足或代价过高的需求，要明确提出拒绝或替代方案，不要无限迎合用户。

## 任务编排

- 默认角色拓扑为 `pm -> builder -> tm -> coder/tester/opser`。
- `pm` 只负责和用户沟通、澄清需求、做最终裁决，不直接修改代码。
- `builder` 只接受已经完成需求确认的任务，负责把需求拆成带序号的小任务和依赖关系，只输出设计结果，不改源码。
- `tm` 负责单个子任务的串行编排，先后驱动 `coder`、`tester`、`opser`；必须记录每次下发、接收、确认、重试和退出事件。
- `coder`、`tester`、`opser` 只处理 `tm` 下发的单个子任务，不直接与用户通信。
- `coder` 完成后必须先把实现结果、失败原因或阻塞原因通知给 `tm`，再退出。
- `tester` 无论通过与否，都必须先把测试结论、失败点或阻塞原因通知给 `tm`，再退出。
- `opser` 无论通过与否，都必须先把收口结论、失败原因或阻塞原因通知给 `tm`，再退出。
- `tm` 在 `opser` 失败时重启 `opser`，重试序号递增；连续失败 3 次时请求人工介入。
- 每一层都要留下“发出 + 确认”两段式回执：上游写入结果，下游写入已接收/已处理确认，避免只有结果没有接收方痕迹。
- 每条回执必须包含 `event_id`、`reply_to`、`correlation_id`、`state`；`reply_to` 必须指向已存在的上游事件。
- 任何角色只能按 `track/README.md` 定义的状态转移表推进，非法跳转必须拒绝并记为 `blocked`。
- `pm` 只消费 `builder` 与 `tm` 的结构化摘要，不吸收 `coder/tester/opser` 的原始过程噪声。
- 目录和文件命名必须使用固定前缀、零填充序号和角色唯一后缀，避免同目录同名覆盖。
- 运行记录统一写入 `track/`，按 `run_id`、`task_id`、`attempt_id` 分层，保持追加式、可追踪、按时间排序。
- `push`、`PR`、`merge` 先按禁用状态保留字段和流程，不默认启用。

## 执行规则

- 默认允许工作区内的阅读、编辑、测试、检查和常规 git 操作。
- 安装软件或安装包视为单独审批项，先确认再执行。
- 变更应尽量保持最小、清晰、可追踪。
