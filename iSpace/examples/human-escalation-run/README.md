# 人工介入运行

本示例说明何时升级人工介入。

触发条件：

- 连续失败达到 profile 的 `max_attempts`。
- 子级明确返回 `blocked` 且缺少自动恢复路径。
- 审计发现敏感信息、越权路径或缺失关键回执。
- 外部发布、合并、部署或远端写入需要用户确认。

预期 track 形态：

- 记录升级原因。
- 记录已尝试动作。
- 记录需要人工裁决的问题。
- 记录用户确认依据，不记录隐藏指令或敏感信息。

人工介入不是绕过流程。人工确认后，后续 worker 仍应通过直属上级接收任务，并继续写入结构化结果。
