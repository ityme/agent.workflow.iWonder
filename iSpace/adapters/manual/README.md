# Manual Adapter

manual adapter 用于必须人工确认或人工执行的步骤。

使用时机：

- 任务需要审批、线下验证或人工判断。
- 目标环境不允许自动执行命令。
- 调度器需要保留结构化事件，但不直接启动 worker。

约束：

- 人工结论必须写回 `track`。
- 仍需提供 `result`、`ack_result`、证据引用和下一步。
- 不得把未筛选的聊天记录、凭据或原始噪声日志写入用户可见事件。
